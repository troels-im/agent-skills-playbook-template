# Tracker Provider Policy

Trace manifests support these tracker providers:

- `github`
- `gitlab`
- `linear`
- `local`

Each `trace.yaml` must include a provider, id, url, project, and title under
`tracker:`.
