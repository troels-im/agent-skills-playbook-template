# Skill Spec Policy

Every `SKILL.md` under `.agents/skills/` must have YAML front matter with:

- `name`
- `description`

Keep the body concise and route readers to indexes, playbooks, references,
scripts, or assets for deeper context.
