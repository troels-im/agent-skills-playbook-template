# TODO Format Policy

Task markers must point to a durable ticket or trace id.

Use:

```text
TODO(ticket_id): description
FIXME(ticket_id): description
```

Examples:

```text
TODO(github-123): remove compatibility alias after downstream repos migrate.
FIXME(linear-ENG-123): handle empty tracker response.
```
