# Docs Index

Human-facing documentation belongs here.

Keep this index short and point to the smallest useful document. Agent-facing
workflow instructions belong under `playbooks/`, while repo orientation belongs
in `index.md` files.

## Documents

- `source-playbook-selection.md`: Explains which source playbook patterns were
  included in this template and which were left for consuming repositories.
- `traceability-model.md`: Defines tickets, traces, requirements, tasks,
  checked-in artifacts, and commit trailers.
